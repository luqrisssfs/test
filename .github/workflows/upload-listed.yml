name: Accion con Desplegable

on:
  workflow_dispatch:
    inputs:
      opcion:
        description: 'Selecciona una accion'
        required: true
        default: 'accion1'
        type: choice
        options:
          - accion1
          - accion2
          - accion3

jobs:
  ejecutar_accion:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar accion seleccionada
        run: echo "Has seleccionado: ${{ github.event.inputs.opcion }}"
